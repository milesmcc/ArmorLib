use scan_result::ScanResult;
use binary_object::BinaryObject;

/// Process the given `BinaryObject` and return a `ScanResult`
pub fn process(binary_object: &BinaryObject) -> Result<ScanResult, String> {
    Ok(0)
}
